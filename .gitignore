build/
build-*/
compile_commands.json
data/
# ..except a small committed slice of a real capture. Every feed handler
# needs one: a regression test against synthetic data only ever proves the
# decoder agrees with the generator.
!data/samples/
!data/samples/**
*.hgrm
*.journal
.cache/
